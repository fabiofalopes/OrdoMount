# Ordo Remote Configuration
# List rclone remotes to auto-mount (one per line)
# Comments start with #, empty lines are ignored

# Example remotes (uncomment and modify as needed):
# onedrive-work
# gdrive-personal
# proton-drive

# Add your configured rclone remotes below: