# Ordo Sync Targets Configuration (MIRROR MODE - full local copy, constant sync)
# Format: local_path|remote_path|sync_frequency_seconds|rclone_flags
#
# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ MIRROR MODE: Complete bidirectional sync - local copy always matches cloud │
# │ Use this for folders you need OFFLINE and constantly synced                 │
# │ Daemon scans and syncs automatically at the specified interval              │
# └─────────────────────────────────────────────────────────────────────────────┘
#
# Examples:
#   /home/user/Documents|onedrive:Documents|300|
#   /home/user/SharedProject|gdrive:SharedProject|180|--drive-shared-with-me
#
# Commands:
#   Add new target:    ./scripts/ordo-sync.sh init <local> <remote> <freq> [flags]
#   Force sync now:    ./scripts/ordo-sync.sh sync
#   Check status:      ./scripts/ordo-sync.sh status
#
# Stop mirroring a folder:
#   1. Comment out or delete its line below
#   2. Local files remain (just stops syncing)
#   3. Delete local folder if you want: rm -rf <local_path>
#   4. Cloud copy is NOT affected
#
# Flags (4th column):
#   --drive-shared-with-me  : For Google Drive "Shared with me" folders
#   (leave empty for normal folders)

/home/fabio/Documents|onedrive-f6388:Documents/|100|
